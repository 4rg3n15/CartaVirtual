<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CalificarActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FEFAE0"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Campo para escribir reseña -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="match_parent"
            android:layout_height="397dp"
            app:srcCompat="@drawable/eden_hall" />

        <EditText
            android:id="@+id/reviewText"
            android:layout_width="match_parent"
            android:layout_height="116dp"
            android:hint="¿Como fue tu experiencia en el EdenHall? (Esta reseña sera anónima"
            android:inputType="textMultiLine"
            android:minLines="3"
            android:textColor="#283618" />

        <!-- RatingBar para calificación -->
        <RatingBar
            android:id="@+id/ratingBar"
            android:layout_width="wrap_content"
            android:layout_height="49dp"
            android:numStars="5"
            android:stepSize="1.0" />

        <!-- Botón para enviar reseña -->
        <Button
            android:id="@+id/submitButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="#BC6C25"
            android:rotationX="4"
            android:text="Enviar al edén" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <Button
            android:id="@+id/backToMainButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_margin="16dp"
            android:backgroundTint="#E08B3F"
            android:rotation="0"
            android:rotationX="6"
            android:rotationY="11"
            android:text="Regresar"
            android:textColor="@android:color/white"
            app:icon="@drawable/back" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>